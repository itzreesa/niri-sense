#[derive(Debug, Clone)]
pub enum SenseVibrationLength {
  Short,
  Medium,
  Long,
  CustomA,
  CustomB,
}
#[derive(Debug, Clone)]
pub enum SenseVibrationStrength {
  Low,
  Medium,
  High,
  CustomA,
  CustomB,
}
